<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.online">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="online"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Addinfo" />
        <activity android:name=".Addinfo_history" />
        <activity android:name=".DisplayListView" />
        <activity android:name=".DisplayListView2" />
        <activity android:name=".DisplayListView3" />
        <activity android:name=".jason" />
        <activity android:name=".jason_appointment" />
        <activity android:name=".jason_fullHistory" />
        <activity android:name=".MainActivity"/>
        <activity
            android:name=".patient"
            android:label="@string/title_activity_bc"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".RegisterActivity"/>

    </application>

</manifest>